#include <stdio.h>
/**
 * main - prints all possible combinations of two digits
 * Return: Always 0 (Success)
 */
int main(void)
{
	int digt1;
	int digt2;

	for (digt1 = 0; digt1 < 9; dift1++)
	{
		for (digt2 = digt1 + 1; dig2 < 10; digt2 )
	}
}
